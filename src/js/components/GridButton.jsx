const React = require('react');

let GridButton = React.createClass({
  getInitialState() {
    return {};
  },

  componentDidMount() {
  },

  render() {
    return (
      <p>Hello, world!</p>
    );
  }
});

module.exports = GridButton;
